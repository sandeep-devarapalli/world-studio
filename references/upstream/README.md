# Upstream Reference Clones

This directory is intentionally ignored by Git.

Use `scripts/clone-upstreams.mjs` to create shallow local reference clones for study. The
public Apache 2.0 repo tracks only `docs/upstreams.md` so incompatible or heavyweight
upstream code does not accidentally ship in World Studio.


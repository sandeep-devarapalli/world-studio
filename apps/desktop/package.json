{
  "name": "@world-studio/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && WORLD_STUDIO_RENDERER_URL=http://127.0.0.1:5173 electron .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@world-studio/world-core": "workspace:*",
    "electron": "43.0.0"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
